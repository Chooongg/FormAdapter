<resources>
    <!-- Base application theme. -->
    <style name="Theme.FormAdapter2" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>